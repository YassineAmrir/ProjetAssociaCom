package com.AssociaCom.helloworld.service;

import lombok.AllArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@AllArgsConstructor
public class RegistrationService {
    /*
    private final AssociationService associationService;
    private final PresidentService presidentService;


    public boolean handle(Association association){

        boolean isAssociationCreated = associationService.create(registrationFormDto.getAssociation());
       //boolean isPresidentCreated = presidentService.create(registrationFormDto.getPresident());
        return isAssociationCreated ;
                //&& isPresidentCreated;
    }

     */
}
